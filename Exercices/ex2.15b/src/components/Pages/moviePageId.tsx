import React from 'react';

import MovieId from '../movieItem/movieId';

const MoviePageId: React.FC = () => {
    

    return (
        <div>
          <MovieId/>
        </div>
    );
};

export default MoviePageId;